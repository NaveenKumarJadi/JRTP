spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/sbms
    driver: com.mysql.cj.jdbc.driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
